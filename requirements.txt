pandas>=2.2
numpy>=1.26
pyarrow>=16.0
scikit-learn>=1.5
scipy>=1.13

# embeddings + sentiment models
torch>=2.4
transformers>=4.44
sentence-transformers>=3.0
vaderSentiment>=3.3.2

# local 4-bit LLM for the RAG agent (runs on the RTX 4060, ~5.5 GB VRAM)
accelerate>=0.34
bitsandbytes>=0.44
hf-transfer>=0.1.8

# app + charts
streamlit>=1.38
plotly>=5.24

# rag agent talks to the local ollama server over http
requests>=2.32
tqdm>=4.66
